# HAVI repro pages

Ad-hoc minimal repro pages used during renderer and screenshot investigations.

These are not the canonical exact screenshot cases. The canonical checked-in
reftests live under `reftest/`.

Use these pages when you need a live page for DevTools probing or to build a
new reduced case before promoting it into `reftest/` or a WPT-oracle manifest.
